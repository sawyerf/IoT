---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app_one
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app_one
            port:
              number: 80

---
apiVersion: v1
kind: Service
metadata:
  name: app_one
spec:
  ports:
    - port: 80
      protocol: TCP
  selector:
    app:  app_one

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app_one-nginx
spec:
  selector:
    matchLabels:
      app: app_one
  replicas: 3
  template:
    metadata:
      labels:
        app: app_one
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: app_one-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: app_one-volume
        configMap:
          name: app_one
